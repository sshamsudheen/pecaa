<h1>Listing product_vendors</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Is active</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_vendors.each do |product_vendor| %>
  <tr>
    <td><%= product_vendor.name %></td>
    <td><%= product_vendor.is_active %></td>
    <td><%= link_to 'Show', product_vendor %></td>
    <td><%= link_to 'Edit', edit_product_vendor_path(product_vendor) %></td>
    <td><%= link_to 'Destroy', product_vendor, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product vendor', new_product_vendor_path %>
