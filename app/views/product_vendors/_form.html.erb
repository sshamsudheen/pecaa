<%= form_for(@product_vendor) do |f| %>
  <% if @product_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_vendor.errors.count, "error") %> prohibited this product_vendor from being saved:</h2>

      <ul>
      <% @product_vendor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :is_active %><br />
    <%= f.check_box :is_active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
